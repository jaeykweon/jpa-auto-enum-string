package com.example.myapp;

// Dummy class used in AutoEnumStringConfigTest to verify isInBasePackage().
// A real class is required because the API takes a Class<?> object, not a string.
public class Dummy {
}
